import {StyleSheet} from 'react-native';
import {Color} from '../../Constant';

export const styles = StyleSheet.create({
  termText: {
    fontSize: 12,
    textAlign: 'justify',
    color: Color.DARK,
    marginTop: 15,
  },
  bottomTermText: {
    fontSize: 12,
    textAlign: 'justify',
    color: Color.DARK,
    marginTop: 10,
  },
});
