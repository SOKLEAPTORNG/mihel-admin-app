import {StyleSheet} from 'react-native';
import {Color} from '../../Constant';

export const styles = StyleSheet.create({
  aboutUsText: {
    fontSize: 12,
    textAlign: 'justify',
    color: Color.DARK,
    marginTop: 15,
  },
  bottomAboutUsText: {
    fontSize: 12,
    textAlign: 'justify',
    color: Color.DARK,
    marginTop: 10,
  },
});
