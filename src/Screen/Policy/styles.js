import {StyleSheet} from 'react-native';
import {Color} from '../../Constant';

export const styles = StyleSheet.create({
  title: {
    marginTop: 15,
    marginBottom: 10,
    color: Color.DARK,
    fontWeight: 'bold',
  },
  policyText: {
    fontSize: 12,
    textAlign: 'justify',
    color: Color.DARK,
  },
  bottomPolicyText: {
    fontSize: 12,
    textAlign: 'justify',
    color: Color.DARK,
    marginTop: 10,
  }
});
